const fs = require('fs');

const content = `// ... the actual code`
// wait I can just use create_file since it also overwrites if I use a python script or node script. Let's just create a node script to overwrite the file safely.
